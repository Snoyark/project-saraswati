import styled from "styled-components";


export const UnorderedHorizontalList = styled.ul({
  display: 'flex',
  flexDirection: "row",
  justifyContent: 'space-evenly',
})

export const UnorderedVerticalList = styled.ul({
  display: 'flex',
  flexDirection: "column",
  justifyContent: 'space-evenly',
})
